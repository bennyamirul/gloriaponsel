// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Skema tabel bisnis akan ditambahkan bertahap mulai Phase 1 sesuai roadmap
